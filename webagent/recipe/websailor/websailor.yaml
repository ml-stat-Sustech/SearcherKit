# WebSailor-style WebAgent config.
#
# This keeps the project's WebAgent runtime, but aligns the LLM call kwargs and
# prompt layout with Alibaba DeepResearch WebSailor more closely.

defaults:
  - agent@agent: WebAgent
  - llm@agent.llm_client: OpenAIClient
  - utils@retry_policy: RetryPolicy
  - data_source@data_source: GenericDataSource
  - _self_

retry_policy:
  max_tries: 3

agent:
  llm_client:
    model: WebSailor-7B
    api_key: "EMPTY"
    base_url: http://127.0.0.1:6001/v1
    default_kwargs:
      temperature: 0.6
      top_p: 0.95
      stop:
        - "\n<tool_response>"
        - "<tool_response>"
    concurrency_limit: 512
  parser:
    target: file://recipe/websailor/websailor_parser.py:WebSailorParser
  tools:
    - target: pkg://webagent.tools.tool:GenericMCPTool
      mcp_tool_name: search
      endpoint: http://127.0.0.1:8300/mcp/
      auth_header: ""
      max_concurrency: 512
      response_char_limit: 20000
      enable_trace_logging: true
      raise_on_fatal: true
      final_answer_generator: null
    - target: pkg://webagent.tools.tool:GenericMCPTool
      mcp_tool_name: visit
      endpoint: http://127.0.0.1:8300/mcp/
      auth_header: ""
      max_concurrency: 512
      response_char_limit: 120000
      enable_trace_logging: true
      raise_on_fatal: true
      final_answer_generator: null
  system_prompt: |
    You are a Web Information Seeking Master. Your task is to thoroughly seek the internet for information and provide accurate answers to questions. No matter how complex the query, you will not give up until you find the corresponding information.

    As you proceed, adhere to the following principles:

    1. **Persistent Actions for Answers**: You will engage in many interactions, delving deeply into the topic to explore all possible aspects until a satisfactory answer is found.

    2. **Repeated Verification**: Before presenting a Final Answer, you will **cross-check** and **validate the information** you've gathered to confirm its accuracy and reliability.

    3. **Attention to Detail**: You will carefully analyze each information source to ensure that all data is current, relevant, and from credible origins.
  max_turn: 40
  max_turn_prompt: |
    You have now reached the maximum tool call turns you can handle. You should stop making tool calls and, based on all the information above, think again and provide what you consider the most likely answer in the following format:
    <think>your final thinking</think>
    <answer>your answer</answer>
  max_tokens: 31244
  max_tokens_prompt: |
    You have now reached the maximum context length you can handle. You should stop making tool calls and, based on all the information above, think again and provide what you consider the most likely answer in the following format:
    <think>your final thinking</think>
    <answer>your answer</answer>
  max_tokens_prompt_margin: 500
  llm_retry_policy:
    target: pkg://webagent.utils.retry:RetryPolicy
    max_tries: 5
    exceptions:
      - pkg://webagent.llm.parser:ParsingError
    giveup: null
    jitter: pkg://backoff:full_jitter
    factor: 1.0
    base: 2.0
  tool_retry_policy:
    target: pkg://webagent.utils.retry:RetryPolicy
    max_tries: 5
    exceptions:
      - pkg://webagent.tools.tool:ToolRecoverableError
    giveup: null
    jitter: pkg://backoff:full_jitter
    factor: 1.0
    base: 2.0

data_source:
  source: data/browsecomp_plus_decrypted_qa.jsonl
  input_key: question
  answer_key: answer
  max_items: null

output_path: outputs/websailor_history
overwrite_output: false
